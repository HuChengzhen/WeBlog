<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huchengzhen.weblog.mapper.RefreshTokenMapper">

    <select id="findByRefreshToken" resultType="com.huchengzhen.weblog.dao.RefreshToken">
        select id, user_id, refresh_token, expire_date
        from refresh_token
        where refresh_token = #{refreshToken}
    </select>

    <insert id="insert" parameterType="com.huchengzhen.weblog.dao.RefreshToken">
        insert into refresh_token(id, user_id, refresh_token, expire_date)
        values (null, #{userId}, #{refreshToken}, #{expireDate})
    </insert>
</mapper>